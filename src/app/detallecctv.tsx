import { useEffect } from 'react';

const DetallCctv: React.FC = () => {
  // Aquí puedes agregar el contenido que desees mostrar en esta página
  useEffect(() => {
    // Código de inicialización o carga de contenido adicional si es necesario
  }, []);

  return (
    <div>
      <h1>Información 1</h1>
      <p>Contenido adicional que deseas mostrar.</p>
    </div>
  );
};

export default DetallCctv;